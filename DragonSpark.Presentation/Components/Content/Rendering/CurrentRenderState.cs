namespace DragonSpark.Presentation.Components.Content.Rendering;

/*
sealed class CurrentRenderState : IResult<RenderState>
{
	readonly SessionRenderState _session;

	public CurrentRenderState(SessionRenderState session) => _session = session;

	public RenderState Get() => _session.Get() ?? RenderState.Default;
}*/