@using DragonSpark.Model.Results
@typeparam T

<EqualityRenderGate T="T" Input="@Input" ChildContent="@ChildContent" Refresh="@Refresh" />

@code {
    [CascadingParameter]
    public required T Input { get; set; }

    [Parameter]
    public required RenderFragment<T> ChildContent { get; set; }

    [Parameter]
    public required Switch? Refresh { get; set; }
}
