@typeparam TValue

@inherits Templates.ContentTemplateComponentBase<TValue>

@if (Content is not null)
{
	@ChildContent(Content)
}
else
{
	@NotFoundTemplate
}

@code
{
	[Parameter]
	public TValue? Content { get; set; }
}