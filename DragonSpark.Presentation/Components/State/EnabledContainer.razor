<fieldset disabled="@(!Enabled)" inert="@(!Enabled)">
    @ChildContent
</fieldset>

@code {

    [Parameter]
    public bool Enabled { get; set; } = true;

    [Parameter]
    public required RenderFragment ChildContent { get; set; }
}