@using DragonSpark.Compose
@using DragonSpark.Model.Results

@ChildContent

@code {
    [Parameter]
    public Switch Allowed { get; set; } = null!;

    [Parameter]
    public RenderFragment ChildContent { get; set; } = null!;

    protected override bool ShouldRender() => Allowed || !Allowed.Up();

}
