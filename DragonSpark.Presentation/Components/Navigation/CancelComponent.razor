@using DragonSpark.Presentation.Runtime.Operations
@inject IRequestToken Session

<CascadingValue IsFixed="true" Value="@Session.Get()" ChildContent="@ChildContent" />

@code {
    [Parameter]
    public required RenderFragment ChildContent { get; set; }
}
