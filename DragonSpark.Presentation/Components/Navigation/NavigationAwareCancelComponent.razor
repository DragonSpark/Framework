@inject NavigationManager Navigation

<DragonSpark.Presentation.Components.Navigation.CancelComponent @key="Navigation.Uri" ChildContent="@ChildContent" />
                                                                               
@code {
    [Parameter]
    public required RenderFragment? ChildContent { get; set; }
}