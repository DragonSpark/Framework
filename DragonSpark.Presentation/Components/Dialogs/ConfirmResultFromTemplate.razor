@typeparam T

@inherits ConfirmResultContextComponent<T>

<DialogContext T="T" @ref="_context" Title="@Title" Width="@Width">
    @ChildContent(new(context, Store))
</DialogContext>

@code {
    [Parameter]
    public required RenderFragment<ConfirmRecord<T>> ChildContent { get; set; }
}