namespace DragonSpark.Testing.Runtime
{
	public class SerializerTests
	{
		/*[Theory, MoqAutoData]
		public void Save( Serializer<ClassWithProperty> sut, [Frozen]string property, Class item )
		{
			var data = sut.Save( item );
			Debugger.Break();
		} */
	}
}